#include <iostream>
#include <fstream>
using namespace std;
int main(){
    int a, b;
    fstream input, output;
    input.open("input.txt", ios::in);
    output.open("output.txt", ios::out);
    input >> a >> b;
    output << a + b;
}
